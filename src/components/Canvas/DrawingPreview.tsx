// src/components/Canvas/DrawingPreview.tsx - Cleaned up unused component

import React from 'react';
export const DrawingPreview: React.FC = () => {
  // This component is now only a fallback for the initial line
  // ConveyorDrawingPreview handles the full preview
  return null;
};