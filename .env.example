# Environment variables for the Satisfactory Layout Tool.
#
# This open-source build does not require any environment variables to run.
# This file is preserved as a placeholder for any future configuration.
#
# Copy this file to `.env` and add values as needed. `.env` is gitignored and
# should never be committed.
